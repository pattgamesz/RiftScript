{
  "name": "riftscript",
  "version": "1.1.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "deploy": "node build.js && npx firebase deploy --only hosting",
    "deploy:dev": "node build.js && npx firebase hosting:channel:deploy dev"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  }
}
