{
  "name": "riftscript",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "deploy": "node build.js && npx firebase deploy --only hosting"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  }
}
